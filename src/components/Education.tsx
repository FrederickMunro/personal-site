import styled from "styled-components";

import Venus from '../assets/venus_orbit.jpg';

const Education = () => {

  return(
    <Container>
    </Container>
  )
}

export default Education

const Container = styled.div`
  background-image: url(${Venus});
  width: 100%;
  height: 100%;
  background-size: cover;
  position: fixed;
`