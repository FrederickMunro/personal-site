import styled from "styled-components";

import Mars from '../assets/mars_orbit.jpg';

const Projects = () => {

  return(
    <Container>
    </Container>
  )
}

export default Projects

const Container = styled.div`
  background-image: url(${Mars});
  width: 100%;
  height: 100%;
  background-size: cover;
  position: fixed;
`