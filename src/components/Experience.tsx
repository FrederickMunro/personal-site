import styled from "styled-components";

import Earth from '../assets/earth_orbit.jpg';

const Experience = () => {

  return(
    <Container>
    </Container>
  )
}

export default Experience

const Container = styled.div`
  background-image: url(${Earth});
  width: 100%;
  height: 100%;
  background-size: cover;
  position: fixed;
`